
namespace Am.Collections
{
    class ArrayIterable<T>(private var array: Array<T>) : Iterable<T> {
        import Am.Lang

        fun iterator(): Iterator<T> {
            return new ArrayIterator<T>(this.array) as Iterator<T>
        }
    }
}