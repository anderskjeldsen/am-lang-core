namespace Am.Lang 
{
    class Any 
    {
        primitive fun toString(): String {
            return "Any"
        }

        primitive fun hash(): UInt {
            return 0UI
        }

        primitive fun getClassRef(): ClassRef {
            var classRef = new ClassRef()
            ObjectHelper.initClassRef(this, classRef)
            return classRef
        }
    }
}